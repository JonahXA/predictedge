__pycache__/
*.egg-info/
.pytest_cache/
# Raw HTTP cache is reproducible from the archive + APIs; the curated
# archive under data/archive IS committed (Kalshi's public API only
# retains ~2 months, so the archive is the durable record).
data/cache/
.DS_Store
